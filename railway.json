{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "DOCKERFILE"
    },
    "deploy": {
      "startCommand": "chmod +x entrypoint.sh && ./entrypoint.sh && python manage.py collectstatic --noinput && gunicorn src.wsgi:application --bind 0.0.0.0:8000"
    }
  }
  